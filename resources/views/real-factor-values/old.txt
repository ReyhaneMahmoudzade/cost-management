{{-- @extends('layouts.table-layout') --}}

{{-- @section('title') --}}
    {{-- محاسبات نهایی --}}
{{-- @endsection --}}

{{-- @section('header') --}}
    {{-- @include('partials.header') --}}
{{-- @endsection --}}

{{-- @section('h1', 'محاسبات نهایی ') --}}

{{-- @section('table') --}}

    {{-- <div class="">
        <div class="flex justify-between grid md:grid-cols-4 gap-6">
            <div class="bg-white rounded-xl shadow-lg p-6 mb-6">
                <p class="text-sm text-gray-500"> نام قطعه</p>

                <p class="text-xl text-slate-800 mt-1">
                    {{ $part->code }} - {{ $part->name }}
                </p>
            </div>
            <div class="bg-white rounded-xl shadow-lg p-6 mb-6">
                <p class="text-sm text-gray-500"> هزینه مواد</p>

                <p class="text-xl text-slate-800 mt-1">
                    {{ number_format($result['materialCost']) }}
                </p>
            </div>
            <div class="bg-white rounded-xl shadow-lg p-6 mb-6">
                <p class="text-sm text-gray-500"> هزینه فرآیندها</p>

                <p class="text-xl text-slate-800 mt-1">
                    {{ number_format($result['processCost']) }}
                </p>
            </div>
            <div class="bg-white rounded-xl shadow-lg p-6 mb-6">
                <p class="text-sm text-gray-500"> قیمت تمام شده</p>

                <p class="text-xl text-slate-800 mt-1">
                    {{ number_format($result['totalCost']) }}
                </p>
            </div>
        </div>
    </div>

    <div class="">
        <h2>
            <i class="fas fa-check-circle "></i>
            مواد اولیه
        </h2>
        <div class="bg-white rounded-xl shadow-lg p-6 mb-6">
            <div class="grid md:grid-cols-1 gap-6 ">
                <div class="">
                    <div class="overflow-x-auto">
                        <table class="w-full">
                            <thead class="bg-gray-50">
                                <tr>
                                    <th class="p-3 text-right"> ماده</th>
                                    <th class="p-3 text-right"> مقدار </th>
                                    <th class="p-3 text-right"> قیمت واحد</th>
                                    <th class="p-3 text-right"> هزینه</th>
                                </tr>
                            </thead>
                            <tbody>
                                @foreach ($result['materials'] as $material)
                                    <tr class="border-t">
                                        <td class="p-3">{{ $material['name'] }}</td>
                                        <td class="p-3">{{ $material['quantity'] }}</td>
                                        <td class="p-3">{{ number_format($material['unit_price']) }}</td>
                                        <td class="p-3  font-medium">{{ number_format($material['cost']) }}
                                        </td>
                                    </tr>
                                @endforeach
                            </tbody>
                            <tfoot>
                                <tr class="border-t bg-gray-50 ">
                                    <td colspan="3" class="p-4"> جمع مواد</td>
                                    <td class="p-4 ">{{ number_format($result['materialCost']) }}</td>
                                </tr>
                            </tfoot>
                        </table>
                    </div>
                </div>
            </div>
        </div>
    </div>


    @foreach ($result['processes'] as $process)
        <div class="">
            <h2>
                <i class="fas fa-check-circle "></i>
                فرآیند {{ $process['name'] }}
            </h2>
            <div class=" bg-white rounded-xl shadow-lg p-6 mb-6">
                <div class="grid md:grid-cols-1 gap-6 ">
                    <div class="">
                        <div class="">
                            <h4 class="font-semibold mb-3 text-slate-700 mx-4"> عوامل موثر</h4>
                            <div class="overflow-x-auto">
                                <table class="w-full">
                                    <thead class="bg-gray-50">
                                        <tr>
                                            <th class="p-3 text-right"> عامل</th>
                                            <th class="p-3 text-right"> وزن </th>
                                            <th class="p-3 text-right"> مقدار واقعی</th>
                                            <th class="p-3 text-right"> حاصل ضرب</th>
                                        </tr>
                                    </thead>
                                    <tbody>
                                        @foreach ($process['factors'] as $factor)
                                            <tr class="border-t">
                                                <td class="p-3">{{ $factor['factor'] }}</td>
                                                <td class="p-3">{{ $factor['weight'] }}</td>
                                                <td class="p-3">{{ $factor['value'] }}</td>
                                                <td class="p-3  font-medium">{{ number_format($factor['result'], 3) }}
                                                </td>
                                            </tr>
                                        @endforeach
                                    </tbody>
                                </table>
                            </div>
                        </div>
                        <div class="grid md:grid-cols-4 gap-6 p-5 border-t">
                            <div>
                                <p class="text-sm text-gray-500"> مقدار </p>
                                <p class="font-semibold text-lg">{{ $process['quantity'] }} </p>
                            </div>

                            <div>
                                <p class="text-sm text-gray-500"> نرخ </p>
                                <p class="font-semibold text-lg"> {{ number_format($process['rate']) }} </p>
                            </div>

                            <div>
                                <p class="text-sm text-gray-500"> هزینه پایه</p>
                                <p class="font-semibold text-lg">{{ number_format($process['base_cost']) }}</p>
                            </div>
                            <div>
                                <p class="text-sm text-gray-500"> ضریب نهایی</p>
                                <p class="font-semibold text-lg">{{ number_format($process['coefficient'], 3) }}</p>
                            </div>
                            {{-- <span class="bg-orange-100 text-orange-700 px-3 py-1 rounded-lg text-sm font-semibold">
                                 
                            </span> --}}
                        </div>
                        <div class="flex justify-between items-center border-t px-5 py-4 bg-gray-50">
                            <span class="text-slate-700"> هزینه نهایی فرآیند</span>
                            <span class=" text-xl text-orange-600"> {{ number_format($process['final_cost']) }}</span>
                        </div>
                    </div>
                </div>
            </div>
        </div> --}}
    @endforeach

@endsection

@section('script')
@endsection



















@extends('layouts.table-layout')

@section('title', 'محاسبات نهایی')

@section('header')
    @include('partials.header')
@endsection

@section('h1', 'محاسبات نهایی')

@section('table')

    {{-- Summary Section (Force single row on medium/large screens) --}}
    <div class="grid grid-cols-1 md:grid-cols-4 gap-6 mb-8">
        {{-- قطعه --}}
        <div class="bg-gradient-to-br from-blue-50 to-white rounded-2xl border border-blue-100 shadow-sm p-6 hover:shadow-md transition-all duration-200">
            <p class="text-xs text-blue-600 uppercase tracking-wider font-bold mb-2">نام قطعه</p>
            <p class="text-lg font-bold text-slate-800">
                {{ $part->code }} - {{ $part->name }}
            </p>
        </div>

        {{-- هزینه مواد --}}
        <div class="bg-gradient-to-br from-indigo-50 to-white rounded-2xl border border-indigo-100 shadow-sm p-6 hover:shadow-md transition-all duration-200">
            <p class="text-xs text-indigo-600 uppercase tracking-wider font-bold mb-2">هزینه مواد</p>
            <p class="text-2xl font-black text-indigo-700 font-mono">
                {{ number_format($result['materialCost']) }}
            </p>
        </div>

        {{-- هزینه فرآیندها --}}
        <div class="bg-gradient-to-br from-amber-50 to-white rounded-2xl border border-amber-100 shadow-sm p-6 hover:shadow-md transition-all duration-200">
            <p class="text-xs text-amber-600 uppercase tracking-wider font-bold mb-2">هزینه فرآیندها</p>
            <p class="text-2xl font-black text-amber-700 font-mono">
                {{ number_format($result['processCost']) }}
            </p>
        </div>

        {{-- قیمت تمام شده --}}
        <div class="bg-gradient-to-br from-emerald-50 to-white rounded-2xl border border-emerald-100 shadow-sm p-6 hover:shadow-md transition-all duration-200">
            <p class="text-xs text-emerald-600 uppercase tracking-wider font-bold mb-2">قیمت تمام شده</p>
            <p class="text-2xl font-black text-emerald-700 font-mono">
                {{ number_format($result['totalCost']) }}
            </p>
        </div>
    </div>

    {{-- Materials Section (Structured exactly like processes) --}}
    <section class="mb-8 bg-white rounded-2xl shadow-md border border-gray-100 overflow-hidden">
        <div class="px-6 py-5 border-b border-gray-100 bg-slate-50 flex justify-between items-center">
            <h2 class="text-lg font-extrabold text-slate-800 flex items-center gap-2">
                <i class="fas fa-cubes text-indigo-600 text-xl"></i>
                مواد اولیه
            </h2>
            <span class="text-base font-bold text-indigo-700 bg-indigo-100 px-4 py-2 rounded-xl font-mono shadow-sm">
                جمع کل مواد: {{ number_format($result['materialCost']) }}
            </span>
        </div>
        
        <div class="p-6">
            <div class="overflow-x-auto">
                <table class="w-full text-base text-right">
                    <thead class="bg-slate-100/70 text-slate-700 font-bold">
                        <tr>
                            <th class="px-6 py-4 rounded-r-xl">ماده</th>
                            <th class="px-6 py-4">مقدار</th>
                            <th class="px-6 py-4">قیمت واحد</th>
                            <th class="px-6 py-4 rounded-l-xl">هزینه</th>
                        </tr>
                    </thead>
                    <tbody class="divide-y divide-gray-100">
                        @foreach ($result['materials'] as $material)
                            <tr class="hover:bg-indigo-50/30 transition-colors">
                                <td class="px-6 py-4 font-semibold text-slate-800">{{ $material['name'] }}</td>
                                <td class="px-6 py-4 text-slate-700 font-mono font-medium">{{ $material['quantity'] }}</td>
                                <td class="px-6 py-4 text-slate-700 font-mono font-medium">{{ number_format($material['unit_price']) }}</td>
                                <td class="px-6 py-4 font-bold text-indigo-600 font-mono text-lg">{{ number_format($material['cost']) }}</td>
                            </tr>
                        @endforeach
                    </tbody>
                </table>
            </div>
        </div>
    </section>

    {{-- Processes Section --}}
    @foreach ($result['processes'] as $process)
        <section class="mb-8 bg-white rounded-2xl shadow-md border border-gray-100 overflow-hidden">
            <div class="px-6 py-5 border-b border-gray-100 bg-slate-50 flex justify-between items-center">
                <h2 class="text-lg font-extrabold text-slate-800 flex items-center gap-2">
                    <i class="fas fa-cogs text-amber-600 text-xl"></i>
                    فرآیند: {{ $process['name'] }}
                </h2>
                <span class="text-base font-bold text-amber-700 bg-amber-100 px-4 py-2 rounded-xl font-mono shadow-sm">
                    هزینه نهایی فرآیند: {{ number_format($process['final_cost']) }}
                </span>
            </div>
            
            <div class="p-6">
                <h4 class="font-bold mb-4 text-slate-600 text-sm flex items-center gap-1.5">
                    <span class="w-1.5 h-3 bg-amber-500 rounded-full"></span>
                    عوامل موثر در فرآیند
                </h4>
                <div class="overflow-x-auto mb-6">
                    <table class="w-full text-base text-right">
                        <thead class="bg-slate-100/70 text-slate-700 font-bold">
                            <tr>
                                <th class="px-6 py-3 rounded-r-xl">عامل</th>
                                <th class="px-6 py-3">وزن</th>
                                <th class="px-6 py-3">مقدار واقعی</th>
                                <th class="px-6 py-3 rounded-l-xl">حاصل ضرب</th>
                            </tr>
                        </thead>
                        <tbody class="divide-y divide-gray-100">
                            @foreach ($process['factors'] as $factor)
                                <tr class="hover:bg-amber-50/30 transition-colors">
                                    <td class="px-6 py-4 font-semibold text-slate-800">{{ $factor['factor'] }}</td>
                                    <td class="px-6 py-4 text-slate-700 font-mono font-medium">{{ $factor['weight'] }}</td>
                                    <td class="px-6 py-4 text-slate-700 font-mono font-medium">{{ $factor['value'] }}</td>
                                    <td class="px-6 py-4 font-bold text-amber-600 font-mono text-lg">{{ number_format($factor['result'], 3) }}</td>
                                </tr>
                            @endforeach
                        </tbody>
                    </table>
                </div>

                {{-- Process Metadata Summary (Quantity, Rate, etc.) --}}
                <div class="grid grid-cols-2 md:grid-cols-4 gap-6 pt-5 border-t border-slate-100 bg-slate-50/50 rounded-xl p-4">
                    @foreach ([
                        'مقدار' => ['key' => 'quantity', 'color' => 'text-slate-800'],
                        'نرخ' => ['key' => 'rate', 'color' => 'text-slate-800'],
                        'هزینه پایه' => ['key' => 'base_cost', 'color' => 'text-slate-800'],
                        'ضریب نهایی' => ['key' => 'coefficient', 'color' => 'text-amber-600 font-bold']
                    ] as $label => $meta)
                        <div class="flex flex-col gap-1">
                            <span class="text-xs font-bold text-slate-500">{{ $label }}</span>
                            <span class="text-lg font-mono {{ $meta['color'] }}">
                                {{ is_numeric($process[$meta['key']]) ? number_format($process[$meta['key']], ($meta['key'] == 'coefficient' ? 3 : 0)) : $process[$meta['key']] }}
                            </span>
                        </div>
                    @endforeach
                </div>
            </div>
        </section>
    @endforeach

@endsection

@section('script')
@endsection


















@extends('layouts.table-layout')

@section('title', 'محاسبات نهایی')

@section('header')
    @include('partials.header')
@endsection

@section('h1', 'محاسبات نهایی')

@section('table')

    {{-- Summary Section (Force single row on medium/large screens) --}}
    <div class="grid grid-cols-1 md:grid-cols-4 gap-6 mb-8">
        {{-- قطعه --}}
        <div class="bg-gradient-to-br from-blue-50 to-white rounded-2xl border border-blue-100 shadow-sm p-6 hover:shadow-md transition-all duration-200">
            <p class="text-xs text-blue-600 uppercase tracking-wider font-bold mb-2">نام قطعه</p>
            <p class="text-lg font-bold text-slate-800">
                {{ $part->code }} - {{ $part->name }}
            </p>
        </div>

        {{-- هزینه مواد --}}
        <div class="bg-gradient-to-br from-indigo-50 to-white rounded-2xl border border-indigo-100 shadow-sm p-6 hover:shadow-md transition-all duration-200">
            <p class="text-xs text-indigo-600 uppercase tracking-wider font-bold mb-2">هزینه مواد</p>
            <p class="text-2xl font-black text-indigo-700 font-mono">
                {{ number_format($result['materialCost']) }}
            </p>
        </div>

        {{-- هزینه فرآیندها --}}
        <div class="bg-gradient-to-br from-amber-50 to-white rounded-2xl border border-amber-100 shadow-sm p-6 hover:shadow-md transition-all duration-200">
            <p class="text-xs text-amber-600 uppercase tracking-wider font-bold mb-2">هزینه فرآیندها</p>
            <p class="text-2xl font-black text-amber-700 font-mono">
                {{ number_format($result['processCost']) }}
            </p>
        </div>

        {{-- قیمت تمام شده --}}
        <div class="bg-gradient-to-br from-emerald-50 to-white rounded-2xl border border-emerald-100 shadow-sm p-6 hover:shadow-md transition-all duration-200">
            <p class="text-xs text-emerald-600 uppercase tracking-wider font-bold mb-2">قیمت تمام شده</p>
            <p class="text-2xl font-black text-emerald-700 font-mono">
                {{ number_format($result['totalCost']) }}
            </p>
        </div>
    </div>

    {{-- Materials Section (Structured exactly like processes) --}}
    <section class="mb-8 bg-white rounded-2xl shadow-md border border-gray-100 overflow-hidden">
        <div class="px-6 py-5 border-b border-gray-100 bg-slate-50 flex justify-between items-center">
            <h2 class="text-lg font-extrabold text-slate-800 flex items-center gap-2">
                <i class="fas fa-cubes text-indigo-600 text-xl"></i>
                مواد اولیه
            </h2>
            <span class="text-base font-bold text-indigo-700 bg-indigo-100 px-4 py-2 rounded-xl font-mono shadow-sm">
                جمع کل مواد: {{ number_format($result['materialCost']) }}
            </span>
        </div>
        
        <div class="p-6">
            <div class="overflow-x-auto">
                <table class="w-full text-base text-right">
                    <thead class="bg-slate-100/70 text-slate-700 font-bold">
                        <tr>
                            <th class="px-6 py-4 rounded-r-xl">ماده</th>
                            <th class="px-6 py-4">مقدار</th>
                            <th class="px-6 py-4">قیمت واحد</th>
                            <th class="px-6 py-4 rounded-l-xl">هزینه</th>
                        </tr>
                    </thead>
                    <tbody class="divide-y divide-gray-100">
                        @foreach ($result['materials'] as $material)
                            <tr class="hover:bg-indigo-50/30 transition-colors">
                                <td class="px-6 py-4 font-semibold text-slate-800">{{ $material['name'] }}</td>
                                <td class="px-6 py-4 text-slate-700 font-mono font-medium">{{ $material['quantity'] }}</td>
                                <td class="px-6 py-4 text-slate-700 font-mono font-medium">{{ number_format($material['unit_price']) }}</td>
                                <td class="px-6 py-4 font-bold text-indigo-600 font-mono text-lg">{{ number_format($material['cost']) }}</td>
                            </tr>
                        @endforeach
                    </tbody>
                </table>
            </div>
        </div>
    </section>

    {{-- Processes Section --}}
    @foreach ($result['processes'] as $process)
        <section class="mb-8 bg-white rounded-2xl shadow-md border border-gray-100 overflow-hidden">
            <div class="px-6 py-5 border-b border-gray-100 bg-slate-50 flex justify-between items-center">
                <h2 class="text-lg font-extrabold text-slate-800 flex items-center gap-2">
                    <i class="fas fa-cogs text-amber-600 text-xl"></i>
                    فرآیند: {{ $process['name'] }}
                </h2>
                <span class="text-base font-bold text-amber-700 bg-amber-100 px-4 py-2 rounded-xl font-mono shadow-sm">
                    هزینه نهایی فرآیند: {{ number_format($process['final_cost']) }}
                </span>
            </div>
            
            <div class="p-6">
                <h4 class="font-bold mb-4 text-slate-600 text-sm flex items-center gap-1.5">
                    <span class="w-1.5 h-3 bg-amber-500 rounded-full"></span>
                    عوامل موثر در فرآیند
                </h4>
                <div class="overflow-x-auto mb-6">
                    <table class="w-full text-base text-right">
                        <thead class="bg-slate-100/70 text-slate-700 font-bold">
                            <tr>
                                <th class="px-6 py-3 rounded-r-xl">عامل</th>
                                <th class="px-6 py-3">وزن</th>
                                <th class="px-6 py-3">مقدار واقعی</th>
                                <th class="px-6 py-3 rounded-l-xl">حاصل ضرب</th>
                            </tr>
                        </thead>
                        <tbody class="divide-y divide-gray-100">
                            @foreach ($process['factors'] as $factor)
                                <tr class="hover:bg-amber-50/30 transition-colors">
                                    <td class="px-6 py-4 font-semibold text-slate-800">{{ $factor['factor'] }}</td>
                                    <td class="px-6 py-4 text-slate-700 font-mono font-medium">{{ $factor['weight'] }}</td>
                                    <td class="px-6 py-4 text-slate-700 font-mono font-medium">{{ $factor['value'] }}</td>
                                    <td class="px-6 py-4 font-bold text-amber-600 font-mono text-lg">{{ number_format($factor['result'], 3) }}</td>
                                </tr>
                            @endforeach
                        </tbody>
                    </table>
                </div>

                {{-- Process Metadata Summary (Quantity, Rate, etc.) --}}
                <div class="grid grid-cols-2 md:grid-cols-4 gap-6 pt-5 border-t border-slate-100 bg-slate-50/50 rounded-xl p-4">
                    @foreach ([
                        'مقدار' => ['key' => 'quantity', 'color' => 'text-slate-800'],
                        'نرخ' => ['key' => 'rate', 'color' => 'text-slate-800'],
                        'هزینه پایه' => ['key' => 'base_cost', 'color' => 'text-slate-800'],
                        'ضریب نهایی' => ['key' => 'coefficient', 'color' => 'text-amber-600 font-bold']
                    ] as $label => $meta)
                        <div class="flex flex-col gap-1">
                            <span class="text-xs font-bold text-slate-500">{{ $label }}</span>
                            <span class="text-lg font-mono {{ $meta['color'] }}">
                                {{ is_numeric($process[$meta['key']]) ? number_format($process[$meta['key']], ($meta['key'] == 'coefficient' ? 3 : 0)) : $process[$meta['key']] }}
                            </span>
                        </div>
                    @endforeach
                </div>
            </div>
        </section>
    @endforeach

@endsection

@section('script')
@endsection
