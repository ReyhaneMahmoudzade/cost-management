import './bootstrap';
import 'preline';

import './components/combobox'

import './modules/dynamic-row'

document.addEventListener('DOMContentLoaded', () => {
    window.HSStaticMethods.autoInit();
    window.HSComboBox.autoInit();

});
